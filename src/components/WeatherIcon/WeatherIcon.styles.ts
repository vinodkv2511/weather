import styled from "styled-components";
import { colors } from "../../utils/styleTokens";


export const StyledWeatherIcon = styled.img`
  width: 50%;
  filter: drop-shadow(0 0 0.75rem ${colors.shadow});
`